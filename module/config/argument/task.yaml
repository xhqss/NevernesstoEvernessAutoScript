# ====================
# al-script 任务定义
# ====================
# 格式: {TaskName: [GroupName, ...]}
# 每个任务只获得列出的参数组 + Storage

Default:
  menu: collapse
  page: setting
  tasks:
    DefaultTask:
    - Device
    - Ocr
    - Window
    - NTEKeyBinding
    - Restart

Tasks:
  menu: collapse
  page: setting
  tasks:
    Daily:
    - Scheduler
    - NTEDaily
    Fishing:
    - Scheduler
    - NTEFishing
    Anomaly:
    - Scheduler
    - NTEAnomaly
    Rhythm:
    - Scheduler
    - NTERhythm
    Heist:
    - Scheduler
    - NTEHeist

GameTools:
  menu: collapse
  page: setting
  tasks:
    AutoCombat:
    - Scheduler
    - NTECombat
    SkipDialog:
    - Scheduler
    - NTEDialog
    FastTravel:
    - Scheduler
    - NTETravel
